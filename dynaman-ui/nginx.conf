server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: Proxy API requests if not using a separate Gateway/load balancer
    # location /api/ {
    #     proxy_pass http://engine:8000/;
    # }
}
